# RDP-Sessions
This PowerShell script is a GUI-based RDP Session Manager built using WPF (Windows Presentation Foundation). It provides an easy and user-friendly way to manage, store, and launch Remote Desktop (RDP) connections.
